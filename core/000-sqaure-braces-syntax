Binds the value of a property of an element into a variable in the .ts file.

<input class="demo" [value]="data.title"/>

[value] is the input property syntax
"data.title" is referring to a object named data with a property of title in the .ts file